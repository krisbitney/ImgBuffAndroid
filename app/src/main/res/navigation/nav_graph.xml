<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/colorizeFragment">

    <fragment
        android:id="@+id/colorizeFragment"
        android:name="com.imgbuff.colorizer.colorize.ColorizeFragment"
        android:label="Colorize"
        tools:layout="@layout/fragment_colorize" >
        <action
            android:id="@+id/action_colorizeFragment_to_historyFragment"
            app:destination="@id/historyFragment" />
    </fragment>
    <fragment
        android:id="@+id/historyFragment"
        android:name="com.imgbuff.colorizer.history.HistoryFragment"
        android:label="fragment_history"
        tools:layout="@layout/fragment_history" />
</navigation>